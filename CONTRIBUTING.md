# Contributing to Management

Requirements: Node.js 22.13+ and npm.

```bash
git clone https://github.com/Inmersion-CS/Management.git
cd Management
npm ci
npm run dev
```

Before a pull request run the dependency audit, typecheck, lint, tests, build, and formatting check. Keep changes focused, tested, accessible, and accurate about implemented versus planned behavior.
