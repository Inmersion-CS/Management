import {render,screen} from '@testing-library/react'; import {describe,expect,it} from 'vitest'; import {App} from './App'; describe('Management foundation shell',()=>{it('presents identity and purpose',()=>{render(<App/>);expect(screen.getByRole('heading',{level:1,name:'Management'})).toBeInTheDocument();expect(screen.getByText('Learn management through planning, decision contexts, coordination, and reflection.')).toBeInTheDocument()});it('provides navigation',()=>{render(<App/>);expect(screen.getByRole('navigation',{name:'Primary navigation'})).toBeInTheDocument();expect(screen.getByRole('link',{name:'Home'})).toBeInTheDocument();expect(screen.getByRole('link',{name:'Roadmap'})).toBeInTheDocument();expect(screen.getByRole('link',{name:'Contribute'})).toBeInTheDocument()});it('renders not-found',()=>{window.location.hash='#/missing';render(<App/>);expect(screen.getByRole('heading',{name:'Page not found'})).toBeInTheDocument();expect(screen.getByRole('link',{name:'Return home'})).toBeInTheDocument()})});
